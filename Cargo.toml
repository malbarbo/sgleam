[workspace]
members = ["engine", "tests", "cli", "wasm"]
default-members = ["engine", "cli"]
resolver = "2"

[profile.release]
opt-level = 2
lto = true
codegen-units = 1

[profile.release-small]
inherits = "release"
opt-level = 's'
lto = true
codegen-units = 1
panic = "abort"
